package understandinginterface;

public interface ChildInterface extends Father, Mother {
// Scenario 1 : implements keyword: when a class wants to implement/inherit from interface we use implements keyword
// Scenario 2 : extends keyword: when a class wants to extend/inherit from another class we use extends keyword
// Scenario 3 : extends keyword: when an interface wants to extend/inherit from another interface we use extends keyword

}
